language: node_js
node_js:
  - '0.8'
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-react-quickstart' ]; then cd .. && eval "mv $currentfolder generator-react-quickstart" && cd generator-react-quickstart; fi

